name: AgentScope
options:
  bundleIdPrefix: com.agentscope
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

packages:
  swift-collections:
    url: https://github.com/apple/swift-collections.git
    from: "1.1.0"

targets:
  AgentScope:
    type: application
    platform: macOS
    sources:
      - path: AgentScope
        excludes:
          - Info.plist
      - path: CHANGELOG.md
        type: file
        buildPhase: resources
    info:
      path: AgentScope/Info.plist
      properties:
        LSUIElement: true
        CFBundleIconFile: AppIcon
        CFBundleName: AgentScope
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
    dependencies:
      - package: swift-collections
        product: OrderedCollections
    entitlements:
      path: AgentScope/AgentScope.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.files.user-selected.read-only: true
    settings:
      base:
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        PRODUCT_BUNDLE_IDENTIFIER: com.agentscope.app
        CODE_SIGN_STYLE: Manual
        CODE_SIGN_IDENTITY: "Developer ID Application"
        DEVELOPMENT_TEAM: "DN8M2CQ4D2"
        PRODUCT_NAME: AgentScope
        GENERATE_INFOPLIST_FILE: false
        INFOPLIST_FILE: AgentScope/Info.plist
        CODE_SIGN_ENTITLEMENTS: AgentScope/AgentScope.entitlements
        ENABLE_HARDENED_RUNTIME: true
        OTHER_CODE_SIGN_FLAGS: "--timestamp"
        OTHER_LDFLAGS: "-lsqlite3"
      configs:
        Release:
          CODE_SIGN_INJECT_BASE_ENTITLEMENTS: false

  AgentScopeTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: AgentScopeTests
    dependencies:
      - target: AgentScope
    settings:
      base:
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        GENERATE_INFOPLIST_FILE: true
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGNING_REQUIRED: false

schemes:
  AgentScope:
    build:
      targets:
        AgentScope: all
        AgentScopeTests: [test]
    test:
      targets:
        - AgentScopeTests
